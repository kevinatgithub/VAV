import { Tag as TagRaw } from '@blueprintjs/core';
import styled from 'styled-components';

const Tag = styled(TagRaw)`
  line-height: 1;
  height: 20px;
  width: 80px;
  text-align: center;
`;

export default {
  Tag,
};
