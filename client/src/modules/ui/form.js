import styled from 'styled-components';
import {
  widthProps,
  heightProps,
  borderProps,
} from './styling/styling';

const Form = styled.form`
  ${widthProps};
  ${heightProps};
  ${borderProps};
`;

export default Form;
